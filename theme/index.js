const { colors } = require('./colors');
const { spacing } = require('./spacing');
const { fonts, fontSizes, fontWeights } = require('./fonts');
const { radius } = require('./radius');
const { shadows } = require('./shadows');
const { sizes } = require('./sizes');
const { bannerStyles, bannerOverlayClass } = require('./bannerStyles');

module.exports = {
  colors,
  spacing,
  fonts,
  fontSizes,
  fontWeights,
  radius,
  shadows,
  sizes,
  bannerStyles,
  bannerOverlayClass,
};
